# Copy this file to .env and fill in values only used by tooling or a server.
# Never commit the resulting .env file.
#
# Firebase Android/iOS client configuration intentionally does not go here:
# it is bundled into the app and must be protected through Firebase settings.

# Example server-side or development-only value:
# APP_CHECK_DEBUG_TOKEN=
